GitHub -> VersionControlSystem
COMMIT -> am adaugat ceva care merge

> git init -> initializeaza

> git status :branch

> git commit : 
	- STAGING AREA: > git add <...>    (sau -A = all)
	- BRANCH: > git commit    (pleaca in cloud)
	
	
> git log : arata detaliile despre commit-ul curent

> git commit -m "Exemplu titlu"

Dupa X commit-uri, este nevoie de >git push pentru upload pe server
> git remove -v
> git push origin master
			branch pe server, branch local